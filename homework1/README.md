### Project 1

#### Objectives
- Create Makefiles that properly build and install libraries and programs
using the libraries
- Create a directory hierarchy with proper Makefiles to build the programs
and libraries properly at each level of the hierarchy
- Create a simple script (in Perl or Python) to package your hierarchy in
both source and binary form.
- Build source files and Makefiles according to a supplied style guide.

